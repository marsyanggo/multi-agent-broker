# Session Journal — multi-agent-broker

_Auto-generated by /save. Newest entries at the top._

---

## Session: 2026-05-19 23:33 PDT

### 完成的 tasks
- T1. Project skeleton — `pyproject.toml`, 目錄結構, CLI entry points  _(Goal: Phase 1)_
- T2. Shared models + protocol — Agent / Message / Task Pydantic models + WS envelope  _(Goal: Phase 1)_
- T3. SQLite schema + CRUD — 含 atomic task claim  _(Goal: Phase 1)_
- T4. FastAPI app + API key auth middleware  _(Goal: Phase 1)_
- T5. REST routes — agents / messages / tasks 三組  _(Goal: Phase 1)_
- T6. WebSocket Hub — 連線註冊、路由、心跳、離線訊息暫存 7 天  _(Goal: Phase 1)_
- T7. MCP broker_client — WS 客戶端 + 指數退避重連  _(Goal: Phase 1)_
- T8. MCP server + 9 個 tools  _(Goal: Phase 1)_
- T9. Pending-count 攔截器 — tool response 注入 `_pending_messages`  _(Goal: Phase 1)_
- T10. 整合測試 — localhost 兩個 MCP server 跑通 demo 4 個情境  _(Goal: Phase 1)_

### 詳細紀錄
完成 Phase 1 全部 10 個 sub-task，從零到 broker + MCP agent 雙端跑通。

**架構落地：**
- 中央 broker：FastAPI + SQLite (WAL) + WebSocket Hub
- MCP server：FastMCP-based，9 個 tool（list_agents / get_agent_info / report_status / send_message / get_messages / create_task / claim_task / update_task / list_tasks）包 BrokerClient
- BrokerClient：httpx REST + websockets WS，背景指數退避重連（1→2→4→...60s）

**關鍵設計決策：**
- 專案命名定為 `multi-agent-broker`（原 `claude-collab` 因目標 LLM-agnostic 而改）
- Push 限制處理：MCP tool response 統一包裝為 `{result, _pending_messages, _pending_task_events}`，LLM 每次 tool call 都被動知道有東西要拉
- Agent name 衝突：gen-key 時報錯預設，CLI 加 `--auto-suffix` 自動編號（builder → builder-2）
- API key：`mab-ak-` prefix + 24-byte token_urlsafe，broker 端只存 SHA-256 hash
- Task claim 競態：用 `UPDATE ... WHERE status='pending' RETURNING` 保證單一 winner
- WS connect 流程先送 self-`agent_event(online)` 給連線者本人，消除測試 race + 確認註冊完成

**測試覆蓋：** 51 個測試全綠
- `test_db.py` (6) — schema、CRUD、atomic claim、TTL cleanup
- `test_auth.py` (8) — hash、key 格式、conflict、auto-suffix、Bearer 驗證
- `test_routes.py` (9) — REST 三組 routes E2E
- `test_websocket.py` (8) — WS hub 路由、backfill、agent_event、task_event
- `test_broker_client.py` (6) — 客戶端對真實 uvicorn broker
- `test_mcp_server.py` (10) — MCP tool 註冊 + 邏輯 + T9 pending-count drain 行為
- `test_e2e_demo.py` (4) — 真 subprocess `mab-broker serve` + 2× `mab-agent` MCP stdio，跑通 4 個 demo 情境

### 卡點 / 重要決定
- **T6 WS test hang**：`test_ws_agent_event_offline_broadcast` 在 starlette TestClient 巢狀 WS context 下無法清乾淨，改為單測 online broadcast，offline 廣播驗證留到 T10 真 subprocess（事實證明 T10 4 個情境都通，offline 廣播在生產正常）
- **pytest-asyncio fixture 跨檔案共用**：`live_broker` 原本放在 `test_broker_client.py`，T8 想 import 失敗（`tests/` 不是 package），改放 `conftest.py` 解決
- **`register_agent` 預設 status 從 online 改 offline**：因為 gen-key 時 agent 還沒真的連線，online 是 WS connect 時才設

### 下次從這裡繼續
Phase 1 完成。下一步可選：
1. 把 `mab-broker gen-key --name claude-A` 的 key 設到 Claude Code `.mcp.json`，實際在 Claude Code 裡跑 mab-agent 驗證
2. 開始 Phase 2 規劃：Channels + shared context + broadcast
3. 開始 Phase 3 規劃：Python SDK + OpenAI / Ollama / LangChain adapters

---
